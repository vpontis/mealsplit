Thank you for using MealSplit!
==============================

Summary of your recent meal at <%= @restaurant_name %>:
-------------------------------------------------------

<% @participants.each do |participant| %>
  <%= participant.email %>
  <% participant.food_items.each do |item| %>
	  <%= item.name %>: <%= item.cost_pretty %>
  <% end %>
  Item Total: <%= number_to_currency(participant.subtotal.to_f/100, :unit => "$") %>
  Tax: <%= number_to_currency(participant.tax.to_f/100, :unit => "$") %>
  <% tip = (@meal.tip_percent.to_f/100)*(participant.subtotal.to_f/100) %>
  Tip: <%= number_to_currency(tip, :unit => "$") %>
  Total: <%= number_to_currency(participant.subtotal.to_f/100+participant.tax.to_f/100+tip, :unit => "$") %>

  #####################
  
<% end %>
Yummy yummy!