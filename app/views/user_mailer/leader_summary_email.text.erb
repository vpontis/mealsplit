Thank you for using MealSplit!
==============================

Summary of your recent meal at <%= @meal.restaurant.name %>:
-------------------------------------------------------

<% @meal.participants.each do |participant| %>
  <% participant.food_items.each do |item| %>
    <%= item.name %>: <%= item.cost_pretty %>
  <% end %>

  Item Total: <%= number_to_currency(participant.subtotal.to_f/100) %>
  Tax: <%= number_to_currency(participant.tax.to_f/100) %>
  Tip: <%= number_to_currency(participant.tip(@meal.tip_percent)) %>
  Total: <%= number_to_currency(participant.total.to_f/100 + participant.tip(@meal.tip_percent)) %>

  #####################
  
<% end %>
Yummy yummy!